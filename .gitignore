# Agent context — personal, never committed (sibling GCU convention).
# cradle commits its CLAUDE.md on purpose; this repo does not.
CLAUDE.md
CLAUDE.local.md

# Node
node_modules/
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Note: dist/ is COMMITTED — the bundled single-file build is a vendorable
# artifact (build/bundle.js generates it; CI `build:check` guards drift).

# Editor / OS cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
*.tmp
